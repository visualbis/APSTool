"use strict";var cluster=require("cluster"),numCPUs=require("os").cpus().length;if(cluster.isMaster){for(var i=0;i<numCPUs;i++)cluster.fork();cluster.on("online",function(e){console.log("Worker "+e.process.pid+" is online, expecting: "+numCPUs+" workers.")}),cluster.on("exit",function(e,r,s){console.log("Worker "+e.process.pid+" died with code: "+r+", and signal: "+s),console.log("Starting a new worker"),cluster.fork()})}else cluster.isWorker&&require("./server.js");